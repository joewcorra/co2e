% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize_gas.R
\name{standardize_gas}
\alias{standardize_gas}
\title{Standardize a gas name to its canonical \code{gas_id}}
\usage{
standardize_gas(x)
}
\arguments{
\item{x}{Character vector of gas names as they appear in your data.}
}
\value{
A tibble with columns \code{input}, \code{gas_id} (\code{NA} if unmatched), and
\code{match_type} (\code{"gas_id"}, \code{"synonym"}, or \code{"unmatched"}).
}
\description{
Matches input names against \code{gases$gas_id} first, then against
\code{gas_synonyms$synonym}, using a normalized (case/whitespace/hyphen
insensitive) comparison. No fuzzy matching is performed — an unmatched
name returns \code{NA}, it is never guessed at.
}
\examples{
standardize_gas(c("HFC-134a", "R-134a", "not a real gas"))

}
